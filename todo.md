# Current-Run
- [x] Refactor all Django models to be in their own file. E.g., "models.DashboardWidget" --> "models.dashboard_widget.DashboardWidget".
- [x] Refactor all Django tests to be in their own file / per model organization (ask user for clarification if needed). Tests should also be in a separate top-level folder that mirrors the rest of the directory structure e.g., tests/dashboard/test_dashboard_widget.py
- [x] Use django graphene as the API instead of whatever else you;re doing rn.
- [x] Fix perpetual "loading..." state for dashboard-widgets.

# Later

